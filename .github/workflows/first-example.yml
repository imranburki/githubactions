name: first_action

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: checkout-repo
          uses: actions/checkout@v4
        - name: welcome message
          run: echo "Welcome to GitHub Actions"
        
        - name: List all files
          run: ls

        - name: Read Files
          run: cat README.md  